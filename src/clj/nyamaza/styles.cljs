(ns nyamaza.styles
  (:require [garden.core :as garden]
            [garden.units :as units]
            [garden.selectors :as selectors]
            [garden.stylesheet :as stylesheet]))

(garden/css [:body {:font-family "monospace"}])
